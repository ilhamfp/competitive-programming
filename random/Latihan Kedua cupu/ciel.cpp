#include <bits/stdc++.h>
using namespace std;


int main(){
	
	int T, n, m, tidak, pekerjaan[1050] ;

	scanf("%d", &T);

	for (int i = 0; i < T; ++i)
	{
		scanf("%d %d", &n, &m);
		for (int i = 0; i < n; ++i)
		{
			/* code */
		}

		for (int i = 0; i < m; ++i)
		{
			scanf("%d", tidak);
		}









	}




	return 0;
}